private static Node insertHead(Node head,int val){
        Node temp=new Node(val,head);                 // creating new node(before head) and referencing next as head
        return temp;                                  // returning tempmas new head
